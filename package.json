{
  "name": "phaser3-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "live-server --open=./index.html --port=5173 --quiet"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "patch-package": "^8.0.1",
    "vite": "^5.0.0"
  }
}
